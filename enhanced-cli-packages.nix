pkgs: with pkgs; [
  # Enhanced CLI utilities referenced by fish
  bat          # better cat, but used as better less
  eza          # better ls
  glow         # markdown viewer
  duf          # better df
  delta        # better diff
  tre          # better tree
  fzf          # fuzzy finder
  dust         # better du
  bottom       # better top/htop
  tldr         # better man
  jless        # less for json
  ripgrep      # better grep (rg) 
  
  # useful, but not referenced by fish
  lazygit      # terminal UI for git
  autojump     # for jump (j)
  pay-respects      # command correction
]

